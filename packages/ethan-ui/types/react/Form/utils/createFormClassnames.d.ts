import { FormCommonProps } from "../Form.types";
declare const createFormClassnames: (component: string, props: FormCommonProps, ...classNames: (string | undefined)[]) => string;
export default createFormClassnames;
